# ${app_name}/urls.py
# Generated by uml2django

from django.urls import path, include
#if $settings.UML2DJANGO_GENERATE_REST_APIS
from ${app_name}.rest_api.router import router
#end if

#if $urls_paths
from ${app_name}.views import (
    #for $url_path in  $urls_paths 
    $url_path[1],
    #end for
)
#end if


urlpatterns = [
    #if $settings.UML2DJANGO_GENERATE_REST_APIS
    path('', include(router.urls)),
    #end if
    #for $url_path in $urls_paths:
    path("$url_path[0]", ${url_path[1]}.as_view(), name="$url_path[2]"),
    #end for
]
